<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- MaterialButton Style-->
    <style name="MaterialOutLineButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:paddingLeft">@dimen/mtrl_btn_padding_left</item>
        <item name="android:paddingRight">@dimen/mtrl_btn_padding_right</item>
        <item name="android:strokeColor">@color/colorToolbar</item>
        <item name="android:textColor">@color/colorToolbar</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/text_size</item>
        <item name="strokeWidth">3dp</item>
    </style>

    <!--Floating Button -->
    <style name="FloatingActionButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">bottom|end</item>
        <item name="android:layout_margin">15dp</item>
        <item name="backgroundTint">@color/colorToolbar</item>
        <item name="layout_constraintBottom_toBottomOf">parent</item>
        <item name="layout_constraintRight_toRightOf">parent</item>
    </style>

    <!--Floating Sub Button -->
    <style name="FloatingActionSubButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">bottom|end</item>
        <item name="android:layout_margin">15dp</item>
        <item name="layout_constraintRight_toRightOf">parent</item>
        <item name="android:visibility">invisible</item>
        <item name="backgroundTint">#ffffff</item>
        <item name="elevation">6dp</item>
        <item name="pressedTranslationZ">12dp</item>
    </style>

    <!--Floating Sub Button Explain Text-->
    <style name="FloatingActionSubExplainText">
        <item name="android:background">@drawable/round_theme</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_marginRight">5dp</item>
        <item name="android:visibility">invisible</item>
    </style>

    <!--TableTextView Style-->
    <style name="TableTextViewStyle">
        <item name="android:padding">@dimen/dimens_16dp</item>
    </style>

    <style name="ToolbarStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/cardview_dark_background</item>
        <item name="titleTextColor">#ffffff</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
    </style>

    <!--Dialog Custom Style-->
    <style name="DialogCustomStyle" parent="Theme.MaterialComponents.Light.Dialog">

    </style>
</resources>
